openapi: 3.1.0
info:
  title: oneOf with exactly one null-accepting branch
  version: 1.0.0
paths: {}
components:
  schemas:
    Named:
      type: object
      required: [name]
      properties:
        name:
          type: string
    NullableChoice:
      oneOf:
        - $ref: '#/components/schemas/Named'
        - type: 'null'
